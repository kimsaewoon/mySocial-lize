server:
  servlet:
    encoding:
      charset: utf-8
      force: true
    # url rewrite 문제 해결
    session:
      tracking-modes: cookie
      timeout: 30m
  port: 5000

spring:
  mustache:
    servlet:
      expose-session-attributes: true
      expose-request-attributes: true

  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://${RDS_HOST}:3306/socialize # aws
      username: ${RDS_USERNAME}
      password: ${RDS_PASSWORD}
  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 10
        dialect: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: false

  # m3u8 을 비디오 타입으로 하기위해 추가
  http:
    media-types:
      hls:
        - application/vnd.apple.mpegurl
  data:
    redis:
      host: clustercfg.socializeredis2.xvp0fd.apn2.cache.amazonaws.com
      port: 6379
      password: